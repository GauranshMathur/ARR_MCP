# TEMPLATE — copy to secret.yaml, fill in, and do not commit the result.
#
#   cp secret.example.yaml secret.yaml
#
# Better still, do not keep plaintext keys in the cluster repo at all: generate
# this Secret with Sealed Secrets, External Secrets or SOPS and drop the copy.
#
# Keys here are referenced from the ConfigMap's config.yaml as ${VAR}, and are
# injected into the pod by envFrom in deployment.yaml. An unset or empty value
# is a startup error, so remove the entries for instances you do not run.
apiVersion: v1
kind: Secret
metadata:
  name: arr-mcp
  namespace: media
type: Opaque
stringData:
  SONARR_MAIN_API_KEY: "replace-me"
  SONARR_ANIME_API_KEY: "replace-me"
  RADARR_MAIN_API_KEY: "replace-me"
  PROWLARR_API_KEY: "replace-me"
  BAZARR_TV_API_KEY: "replace-me"
